name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    steps:
      - name: Checkout source
        uses: actions/checkout@v3
      - name: Setup .NET
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: 6.0.x
      - name: Build
        run: |
          dotnet build -c Release
      - name: Test
        run: |
          dotnet test -c Release
      - name: Pack
        run: |
          dotnet pack -c Release -o .\pkg\
      - name: Publish
        run: |
          dotnet nuget push .\pkg\* -k ${{ NUGET_KEY }} -s https://api.nuget.org/v3/index.json
